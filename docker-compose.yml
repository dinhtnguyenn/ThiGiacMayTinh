services:
  faceops:
    build: .
    env_file:
      - path: .env
        required: false
    ports:
      - "8000:8000"
    volumes:
      - faceops-data:/data
      - faceops-models:/models
    restart: unless-stopped

volumes:
  faceops-data:
  faceops-models:
